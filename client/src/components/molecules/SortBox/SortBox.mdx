---
name: SortBox
route: /component/sort-box
menu: Components
---

import { Props } from 'docz'
import SortBox from './SortBox'

# SortBox

- This is a box contains sort radio buttons which is used to sort productList according to defined sort parameters.
- Active sort param obtained from store and displayed accordingly.
- When clicking on an unselected sort button a setter action is dispatched which updates active sort filter.
