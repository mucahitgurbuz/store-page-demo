---
name: Store
route: /component/store
menu: Components
---

import { Props } from 'docz'
import Store from './Store'

# Store

- This is a main page component of the store-page-demo which renders all columns **Filters**, **ProductList**, **Checkout**
- It also dispatch fetch action to get product items on initial render and on every active filter changes.
